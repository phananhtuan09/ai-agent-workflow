# Canonical Skills

This directory contains the upstream source of truth for optional capability skills.
The kit repository must not generate or retain `.agents/skills/` or
`.claude/skills/` mirrors. Runtime adaptation happens only while installing into a
target repository.

After installation, a project may customize its skills.

Do not use an automated sync as a conflict-resolution mechanism.
