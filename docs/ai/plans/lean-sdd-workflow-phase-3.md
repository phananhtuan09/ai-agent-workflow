# Phase 3: Update Existing Files

## Goal
Update review-plan agent, spec-review agent, and .claude/CLAUDE.md
to align with the new lean SDD flow.

## Tasks

### Update .claude/CLAUDE.md
- [ ] [DISCOVER] Read current .claude/CLAUDE.md — note all existing sections
- [ ] Rewrite .claude/CLAUDE.md following the template below (≤ 50 lines)
      Keep any project-specific conventions/constraints already present,
      but restructure into the new format. Remove logging section entirely.

New .claude/CLAUDE.md template (global scope — ≤ 25 lines):
```
# Agent Standards

## Principles
- Simplest solution that meets requirements — no hypothetical futures
- Pre-optimize only for: security (validation, auth) and
  performance (scale, query optimization)
- Unclear → ask first, batch all questions into one block
- One option clearly better → recommend directly
- Options only when decision depends on user's priority —
  state concrete cost of each

## Communication
- Reply in user's language; code and comments in English
- Backticks for `files/functions/classes`
- Status update before/after key actions (1-2 sentences max)

## Workflow
Routing rule: "Does scope need human approval first?"
| Task type                     | Workflow                                |
|-------------------------------|-----------------------------------------|
| New feature                   | /spec → /plan → /execute-plan → /verify |
| Fix bug (clear)               | /plan "Fix: ..." → /execute-plan        |
| Refactor (no behavior change) | /plan "Refactor: ..." → /execute-plan   |
| Fix bug (ambiguous/large)     | /spec → /plan → /execute-plan           |
| Small update (1-2 files)      | /execute-plan "inline task"             |
```
Note: No Structure/Conventions/Constraints/Patterns here — those are
project-scope, generated by /init into the project's CLAUDE.md.

### Update review-plan agent
- [ ] [DISCOVER] Search for review-plan agent: look for "dev-plan-reviewer" or
      "review-plan" in .claude/agents/ and .claude/skills/.
      If not found (deleted in Phase 1) → create .claude/agents/dev-plan-reviewer.md
- [ ] Update the agent to expect new plan format:
      - Plan may be a parent file with ## Phases index pointing to child files
      - If parent file: also read each child phase file before reviewing
      - Criteria unchanged (clarity, completeness, logic, executability)
      - New check: every phase must start with a [DISCOVER] task
      - New check: no file paths in task descriptions (except [DISCOVER] output)
      - New check: every AC from spec must be covered by at least one task

### Update spec-review (review-spec) agent
- [ ] [DISCOVER] Read current spec-review/review-spec agent file
      (search for "review-spec" or "spec-review" in .claude/)
- [ ] Update the agent to expect new spec format:
      - Sections: Problem, Solution, Acceptance Criteria, Out of Scope,
        Open Questions
      - New check: no technology or implementation details in spec
      - New check: each AC is verifiable ("user can X" not "system should Y")
      - New check: Out of Scope section present even if empty
      - New check: Open Questions listed explicitly (not embedded in other sections)
      - Line limit check: warn if spec exceeds 40 lines

## Done when
- .claude/CLAUDE.md updated to new format, ≤ 50 lines, no logging section
- review-plan agent updated with new plan format checks
- spec-review agent updated with new spec format checks
