# Design

<!-- SEED: established with the user before implementation; re-run /impeccable document once there's code to capture the actual tokens and components. -->

> **Unfilled seed.**
> This file records the accepted visual system and has not been filled in yet.
> Run `/impeccable document` once there is shipped UI to read; it derives tokens and components from the built artifact rather than from intentions.
> `docs/decisions/` constrains what this file may record, per `docs/WORKFLOW.md`.

## Colors

## Typography
